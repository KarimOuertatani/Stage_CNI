package com.fitforge.api.common.enums;

/**
 * Systeme d'unites prefere pour l'affichage cote Flutter.
 * METRIQUE = kg/cm (defaut), IMPERIAL = lb/in.
 */
public enum UnitSystem {
    METRIQUE,
    IMPERIAL
}
